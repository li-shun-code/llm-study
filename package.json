{
  "name": "llm-learning-site",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vitepress dev docs",
    "build": "node scripts/renumber.mjs --apply && node scripts/gen-sidebar.mjs docs && node scripts/gen-manifest.mjs && node scripts/check-frontmatter.mjs docs && vitepress build docs",
    "preview": "vitepress preview docs",
    "check": "node scripts/check-frontmatter.mjs docs && node scripts/scan-leaks.mjs docs && node --test tests/*.test.mjs",
    "tts-proxy": "node scripts/tts-proxy.mjs"
  },
  "devDependencies": {
    "@fontsource/noto-sans-sc": "^5.3.0",
    "dompurify": "^3.4.15",
    "marked": "^18.0.13",
    "msedge-tts": "^2.0.7",
    "turndown": "^7.2.4",
    "typescript": "^7.0.2",
    "vitepress": "^1.6.4"
  }
}
